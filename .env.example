# Porta do servidor
PORT=3000

# Segredo para assinatura de sessão
# IMPORTANTE: Gere um segredo único e NUNCA commite o arquivo .env real!
# Para gerar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=sua-chave-secreta-aqui-minimo-32-caracteres

# Ambiente (development ou production)
NODE_ENV=development

# PostgreSQL - Vercel Postgres (será fornecido automaticamente pelo Vercel)
# Deixe em branco localmente, o Vercel injeta automaticamente em produção
POSTGRES_URL=
